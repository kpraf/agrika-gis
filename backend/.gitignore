# Python
venv/
.venv/
__pycache__/
*.pyc

# Local secrets / config
.env

# One-off dev helper that embeds demo passwords in plaintext — keep out of the repo.
scripts/seed_accounts.py

# Setup logs
db/_setup.log
